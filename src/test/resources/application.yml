# Test에서는 이 yml 파일이 우선권을 가진다.
spring:
#  datasource:
##memory DB for test / springboot에서는 datasource, jpa 설정을 다 지워도 알아서 메모리 디비로 돌아간다.
#    url: jdbc:h2:mem:test;
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#     hibernate:
#       ddl-auto: create
#     properties:
#       hibernate:
##         show_sql: true
#         format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace